# Ticket YYYY-MM-DD_NN — <short title>

**Ticket ID:** YYYY-MM-DD_NN
**Date:** YYYY-MM-DD
**Category:** Infrastructure | AD | Lifecycle | Policy | Hybrid
**Environment:** Real — `corp.adlab.local` on Azure (DC01)

## Request

<What was asked, written as if a user or manager requested it. Include requester name/role.>

## Approval

<For a change: approver name/role, and the four-eyes note.>
<For a read-only task: state that no approval is required, and why. Applying change control
where it does not belong is as much a process failure as skipping it where it does.>

## Actions taken

1. <Exact console path or PowerShell command used>
2. ...

## Validation

<The command you ran to prove it worked, and its result. Not "it looked right" - the output.>

```powershell
<command>
```

## Evidence

<!-- Every item needs a caption: WHAT it shows + WHY it was captured (which claim it proves).
     Application window only. No subscription ID, no tenant ID, no VM public IP, no passwords,
     no personal browser data. This repository may become public. -->
- `evidence/<filename>.png` - **What:** <content>. **Why:** <which claim of this ticket it proves>.

## Result & user confirmation

<Outcome. Simulated confirmation where a real user would have confirmed.>

## Simulator comparison

<Optional, and only when there is something to say. If this exercise was also done in the
iam_guide simulator, note where the real directory behaved differently. Those differences are
the most interesting content in this journal - they are what a simulator cannot teach.>

## What I learned

<2-3 lines, own words.>
